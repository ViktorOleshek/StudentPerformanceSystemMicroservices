# Database Configuration
POSTGRES_AUTH_USER=authuser
POSTGRES_AUTH_PASSWORD=authpassword
POSTGRES_AUTH_DB=authdb

POSTGRES_USER_USER=useruser
POSTGRES_USER_PASSWORD=userpassword
POSTGRES_USER_DB=userdb

POSTGRES_COURSE_USER=courseuser
POSTGRES_COURSE_PASSWORD=coursepassword
POSTGRES_COURSE_DB=coursedb

POSTGRES_GRADE_USER=gradeuser
POSTGRES_GRADE_PASSWORD=gradepassword
POSTGRES_GRADE_DB=gradedb

# RabbitMQ Configuration
RABBITMQ_DEFAULT_USER=guest
RABBITMQ_DEFAULT_PASS=guest

# JWT Configuration
JWT_ISSUER=AuthService
JWT_AUDIENCE=MainProject
JWT_KEY=GradeHubSecretKey2025StudentPerformanceSystemJWT256BitKeyForAllServices

# Service URLs (Docker internal network)
AUTH_SERVICE_URL=http://auth:5001
USER_SERVICE_URL=http://users:5269
COURSE_SERVICE_URL=http://courses:5145
GRADE_SERVICE_URL=http://grades:5002
API_GATEWAY_URL=http://api-gateway:5000

# Frontend Configuration
VITE_API_URL=http://localhost:5000
